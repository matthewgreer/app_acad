<h1>99 Cats</h1>
<ul>
<% @cats.each do |cat| %>
    <li> <a href="<%= cat_url(cat) %>"><%= cat.name %></a> - <%= cat.age %> old <%= cat.color %> <%= cat.sex == "M" ? "male" : "female"%> </li>
<% end %>

</ul>


